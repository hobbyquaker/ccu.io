// Write modular code!

require('./pfio.input.changed'); // Watches the Piface inputs for changes
require('./pfio.output.echo'); // Echoes changes to the inputs onto the outputs
require('./pfio.input.hold'); // Adds an event when an input is held for 500ms (try it with the tactile switches on the piface!)